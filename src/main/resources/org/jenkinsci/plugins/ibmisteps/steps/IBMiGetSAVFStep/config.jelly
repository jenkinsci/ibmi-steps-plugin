<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="${%Library}" field="library">
        <f:textbox/>
    </f:entry>

    <f:entry title="${%Name}" field="name">
        <f:textbox/>
    </f:entry>

    <f:entry title="${%ToFile}" field="toFile">
        <f:textbox/>
    </f:entry>
</j:jelly>
